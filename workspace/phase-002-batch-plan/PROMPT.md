This legacy planning directory is retained only for workflow history. Its reviewed output is `outline/batches/volume-01-batch-0001.md`; it does not represent completed chapter prose and must not be run as a writing phase.

The active prose phase is `workspace/volume-01/batch-0001`. Use that directory’s `PROMPT.md` and the reviewed batch outline. The phase ledger is authoritative if this prompt is ever inspected during recovery.
